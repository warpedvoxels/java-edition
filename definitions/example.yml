---
entities:
  - name: Player
    package: org.hexalite.generation.entity
    feature_flag: example
    fields:
      a:
        kotlin: java.util.UUID
        rust: String
      test_c:
        kotlin: Int
        rust: u32
      hello_d:
        kotlin: String
        rust: String
